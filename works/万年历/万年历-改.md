# 修改说明
### 1.添加`checkHoliday()`和`checkHolidayPrint()`，分别用于非文件输出判断节日和文件输出判断节日
### 2.删除`showHoliday()`函数
### 3.`printCalendar(int year, int month)`中添加headDay用于记录天数
### 4.删除时钟函数`sleep()`，防止刷新
### 5.主函数中添加多个`getchar()`和`system('cls')`用来实现完成一次操作后，按回车键刷新屏幕，重新回到开始界面