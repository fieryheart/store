// h1、`Set`API一览

// 1、一个set结构可以由一个数组创建

// 2、`size`是值的数量

// 3、`add()`用来提供给定的值

// 4、`clear()`清楚所有元素

// 5、`delete()`清楚给定的元素

// 6、`entries()`返回一个所有值的迭代器

// 7、`forEach`对于每个值调用一个回调函数

// 8、`has()`返回true，如果给定的值存在于这个set数据结构中

// 9、`keys()`、`values()`、`[Symbol.iterator]()`都和`Map`结构差不多