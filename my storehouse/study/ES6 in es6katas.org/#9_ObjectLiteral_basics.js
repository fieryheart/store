// h1、对象字面值(object-literals)

// 1、简单的表示——`{x: x}`等于{x}

// 2、对象中增加更多的属性也是一样的

// 3、在对象的方法中，只能用其key值作为字面值

// 4、 一个不同的关键字就必须特殊写明，想ES6之前的版本一样

// 5、在内联函数中，可以用`obj = { func(){} }` 代替 ` obj = {func:function(){}} `