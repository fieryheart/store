// h1、`map.has()`支出一个key是否存在

// 1、在空的map中找不到任何东西

// 2、通过key查到一个元素

// 3、key为undefined也可以

// 4、不会强制转换key

// 5、在移除(使用`map.delete(<key>)`)一个之后就再也找不到那个元素

// 6、增加一项(使用`mao.set(key, value)`)之后将会让`has()`返回正确